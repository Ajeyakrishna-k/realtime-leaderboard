import styles from "./leaderboard-header.module.css";

const LeaderboardHeader = () => {
  return (
    <header className={styles.header}>
      <h1 className={styles.title}>Leaderboard</h1>
    </header>
  );
};
export default LeaderboardHeader;
